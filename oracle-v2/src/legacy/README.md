# Legacy Oracle Assets

This folder contains pre-Hono fallback assets kept for compatibility:

- `server-legacy.ts`: old single-file HTTP server entrypoint
- `ui.html`: legacy Oracle UI
- `dashboard.html`: legacy dashboard HTML
- `arthur.html`: legacy Arthur chat UI

The active runtime entrypoint is `src/server.ts` and modern frontend build in `frontend/`.
